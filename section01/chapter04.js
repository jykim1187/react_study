// 1.변수
let age;

age=30;

// 2.상수(const는 값이 변할 수 없기 때문에 처음 선언할 때 반드시 초기화가 이루어져야 함)
const birth ="1997.01.07";

// 3.변수 명명규칙(네이밍 규칙)
// 3-1. $,_제외한 기호는 사용할 수 없다(#같은 다른 기호는 아예 불가)
let $_name;

//3-2. 숫자로 시작할 수 없다.
let name;

//3-3. 예약어(let,const,if 등)를 사용할 수 없다.

// 4.변수 몀명 가이드

